import React from "react";
import { Text, View } from "react-native";

type Props = {};

const DeviceUpdate = (props: Props) => {
  return (
    <View>
      <Text>DeviceUpdate</Text>
    </View>
  );
};

export default DeviceUpdate;
